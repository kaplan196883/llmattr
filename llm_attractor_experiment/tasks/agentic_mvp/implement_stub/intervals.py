def merge_intervals(intervals):
    """Merge overlapping intervals.

    `intervals` is a list of [start, end] pairs (integers). Return a new
    list of merged, non-overlapping intervals sorted by start. Intervals
    that touch or overlap should be merged (e.g. [1,3] and [2,6] ->
    [1,6]; [1,2] and [2,3] -> [1,3]).

    NOT YET IMPLEMENTED.
    """
    raise NotImplementedError
